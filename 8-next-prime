public Integer nextPrime(Integer num) {
    //code here
    // num=+1;
    while(true){
        for(integer i=1;i<num/2;i++){
            if(math.mod(num,i)==0){
                break;
        num+=1;
            }
        }
        return num;
    }
    return num;
}