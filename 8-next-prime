public Integer nextPrime(Integer num) {
    //code here
    // num=+1;
    while(true){
        for(integer i=1;i<num/2;i++){
            if(math.mod(num,i)==0){
        num+=1;
                break;
            }
        }
        return num;
    }
    return num;
}