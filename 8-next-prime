public Integer nextPrime(Integer num) {
    //code here
    // num=+1;
    while(true){
            num+=1;
        for(integer i=2;i<num/2;i++){
            if(math.mod(num,i)==0){
                break;
            }
        }
        return num;
    }
    return num;
}